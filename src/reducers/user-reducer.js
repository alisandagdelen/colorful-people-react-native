import * as types from '~/src/actions/types'

const initialState = {
  currentUser: 'ozan',
};

export default function(state = initialState, action) {
  switch(action.type) {

    default:
      return state
  }
}